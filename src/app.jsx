import React, { Component } from 'react';
import './app.css';

export default class componentName extends Component {
  render() {
    return (
      <div> React + Webpack + Babel = ❤️ </div>
    );
  }
}
